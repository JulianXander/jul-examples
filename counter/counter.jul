# jul counter.jul
counter$ = timer$(1000f)
counter$.subscribe(
	(x) =>
		log(x)
		runJs(§document.body.innerText = §(x)§)
		x ?
			10f => counter$.complete()
			() => ()
)

# test1 = ValueOf(test)
# test2 = Stream(Float)
# test3 = (delayMs: Float): Stream(Float)