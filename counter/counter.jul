# npx ts-node src/cli.ts ../jul-examples/counter/counter.jul
counter$ = timer$(1000)
counter$.subscribe(
	(x) =>
		log(x)
		runJs(§document.body.innerText = §(x)§)
		x ?
			10 => counter$.complete()
			() => ()
)

# test1 = ValueOf(test)
# test2 = Stream(Float)
# test3 = (delayMs: Float): Stream(Float)