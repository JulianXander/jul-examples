myList = (
	§a§
	§b§
	§c§
)
forEach(myList (value key) => log(key value))
forEach(myList (key value) =>
	log(key value))
forEach(
	myList
	(key value) =>
		log(key value)
)


complexObj = (a = myList c = add(4 5) [§ %§] = §komischer wert§ t2 = void)

# Template String:
someVar = §Welt§
template = §hallo §(someVar)§
# TODO Interpolation ohne Klammern?
# template = §hallo §someVar§
# $$ ist escapeSequenz
testStr = §hallo §§ para§