inline = §Hallo Welt§

mitInterpolation = §Hier ist §(inline) ...§

multiLine = §
	Das ist
	ein mehrzeiliger
		String!

	# Kommentare eingerückt
	mit §(mitInterpolation)
	§§ Escape Sequenz für Paragraph
	§# Escape Sequenz für Raute
§

multiLineHtml = §html
	<div>
		Hallo §(inline)
	</div>
§

pageSize = 10
table = §Customer§
sql = §sql
	select top §(pageSize) * from §(table)
	where name like '%test%'
§